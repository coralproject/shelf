package query_test

import (
	"testing"

	"github.com/coralproject/shelf/pkg/tests"
)

// TestQueryExecution validates the process of executing a giving query record
// in the db.
// func TestQueryExecution(t *testing.T) {
// 	tests.ResetLog()
// 	defer tests.DisplayLog()
//
// 	t.Logf("Given the need to execute a query.")
// 	{
// 	}
// }

// TestPipelineQueryExecution validates the process of executing a giving query record
// in the db.
func TestQueryExecution(t *testing.T) {
	tests.ResetLog()
	defer tests.DisplayLog()

	t.Logf("Given the need to execute a query.")
	{
	}
}
